[build-system]
requires = ["setuptools>=46.4.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cwordtm"
authors = [{name = "NLP 2024", email = "nlp202403@gmail.com"}]
dynamic = ["version", "description"]
